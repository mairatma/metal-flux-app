{namespace Templates.App}

/**
 * This renders the main element.
 * @param? elementClasses
 * @param id
 * @skipUpdates elementClasses
 */
{template .content}
	<div id="{$id}" class="app component{$elementClasses ? ' ' + $elementClasses : ''}">
	</div>
{/template}
