{namespace Templates.AppProfile}

/**
 * @param id
 * @param user
 */
{template .content}
	<div id="{$id}" class="app-profile">
		<span class="avatar-photo"><img src="{$user.photo}"></span>
		<h3 class="app-profile-name">{$user.name} <small>"{$user.status}"</small></h3>
	</div>
{/template}
